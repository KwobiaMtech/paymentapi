apiVersion: v1
kind: Service
metadata:
  name: postgres-payment-service
  namespace: payment
  labels:
    app: postgres-payment
spec:
  type: NodePort
  ports:
   - port: 5432
  selector:
   app: postgres-payment